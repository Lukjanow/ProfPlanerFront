const debug = {
  active: true
}

const languages = {
  en: "English",
  de: "Deutsch"
}

const i18n = {
  defaultLanguage: "en",
  languageCodes: Object.keys(languages),
  languageNames: languages
}

export {
  debug, i18n
}
